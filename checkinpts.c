#include"shell.h"
/**
* check_inputs_files - checks if a data exists and can be read
* @data: data to check
* This function checks if the data specified by
* @data exists and is readable.
*Return: pointer to @data if it exists
*/
char *check_inputs_files(char *data)
{
char *command;
command = _strup_mem(data);

	return (command);
}
